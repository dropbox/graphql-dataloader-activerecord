# frozen_string_literal: true

RSpec.describe GraphQL::Dataloader::ActiveRecord do
  it "has a version number" do
    expect(GraphQL::Dataloader::ActiveRecord::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
